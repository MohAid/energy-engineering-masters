# Workflow

<div dir="rtl">

قواعد مختصرة تُبقي المستودع مقروءاً ومطابقاً للواقع.

## القاعدة الأولى: لا نص بلا عمل خلفه

لا يُكتب عنوان مشروع، ولا سطر في جدول، ولا بطاقة في صفحة العرض، إلا إذا كان خلفه ملف منجز ومرفوع. المقرر الذي لا مشروع فيه يقال عنه ذلك صراحة، أو لا يُنشأ مجلده أصلاً.

## التسمية

| العنصر | القاعدة | مثال |
|---|---|---|
| مجلد مقرر | `NN-english-slug` بحروف صغيرة وشرطات | `01-advanced-fluid-mechanics` |
| مجلد مشروع | تسمية إنجليزية قصيرة بلا تواريخ | `fanno-flow` |
| ملف كود | حروف صغيرة وشرطة سفلية | `fanno_flow.py` |
| شكل | مرقّم وواصف | `figure-01-naive-vs-pinn.png` |

لا مسافات ولا حروف عربية في أسماء الملفات والمجلدات، وتُستعمل العربية بحرية داخلها.

## الكتابة والكود

- العربية لغة الشرح، والإنجليزية للعناوين والمصطلحات التقنية فقط. لا فقرات مترجمة سطراً بسطر.
- التعليق داخل الكود بضع كلمات عربية، بلا خلط بالإنجليزية في السطر نفسه، لأن المحرر لا يعرض السطر المختلط عرضاً صحيحاً.
- الكود بأبسط شكل يؤدي الغرض: دوال قصيرة مباشرة، بلا طبقات تجريد ولا هندسة زائدة.
- كل نتيجة رقمية تُقارن بمرجع، حلٍّ تحليلي أو حل كتاب، ويُذكر مقدار الفرق وسببه.
- تُذكر حدود العمل صراحة: ما الذي لم يُتحقق منه، وما الذي لا تقوله النتيجة.

## الالتزامات

التزام واحد لتغيير واحد ذي معنى، والرسالة بالإنجليزية بصيغة الأمر:

</div>

```
add fanno flow inlet solver
fix entropy change sign
update pinn results after rerun
```

<div dir="rtl">

والتزم بنهاية كل جلسة عمل، ولا تنتظر اكتمال المشروع.

## الملفات الكبيرة

GitHub يرفض الملفات فوق 100 ميغابايت ويحذّر فوق 50. ملفات الشبكات ونتائج المحاكاة تبقى محلياً ويُشرح مصدرها في `data/raw/README.md`، ويُرفع من النتائج الأشكال والتقارير النهائية فقط.

## المستودع عام

كل ما يُرفع هنا يراه الجميع. قبل كل التزام: هل في هذا بيانات غير منشورة، أو مادة لم يأذن المدرّس بنشرها، أو عمل تمنع النزاهة الأكاديمية نشره؟ إن كان كذلك فلا يُرفع.

ولا تُرفع أبداً كلمات مرور أو مفاتيح وصول أو بيانات شخصية لزملاء أو ملفات برمجيات مرخّصة.

</div>
